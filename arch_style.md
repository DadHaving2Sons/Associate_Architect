### Data Flow

| Quality Attribute | Batch Sequential - Effects | Batch Sequential - Justification | Pipe and Filter - Effects | Pipe and Filter - Justification | Process Control - Effects | Process Control - Justification |
|---|---|---|---|---|---|---|
| Availability | - | 한 단계가 실패하면 뒤 단계가 모두 지연된다. | 0 | 필터는 분리되어 있지만 한 필터 장애가 전체 흐름을 막을 수 있다. | + | 피드백 제어로 이상 상태를 감지하고 대응하기 쉽다. |
| Deployability | 0 | 단계별 배포는 가능하지만 배치 순서와 스케줄 의존성이 있다. | + | 필터 단위로 교체하거나 배포하기 쉽다. | - | 하드웨어와 제어 주기에 맞춰 배포 검증이 필요하다. |
| Integrability | + | 파일이나 중간 데이터 형식만 맞추면 단계 연결이 쉽다. | ++ | 표준 입출력 형식으로 여러 필터를 쉽게 조합할 수 있다. | - | 센서, 액추에이터, 제어 로직의 시간 의존성이 강하다. |
| Energy Efficiency | 0 | 대량 처리는 효율적이지만 중간 저장과 반복 I/O가 발생한다. | + | 스트리밍 처리로 불필요한 중간 저장을 줄일 수 있다. | + | 피드백으로 필요한 만큼만 동작하게 조절할 수 있다. |
| Modifiability | + | 특정 단계만 수정해도 전체 구조 영향이 비교적 작다. | ++ | 필터를 추가, 제거, 교체하기 쉽다. | - | 제어 로직 변경은 안정성과 안전성 재검증이 필요하다. |
| Performance | 0 | 대량 처리에는 유리하지만 순차 실행이라 응답 시간은 느리다. | + | 파이프라인 병렬 처리로 처리량을 높일 수 있다. | ++ | 실시간 응답과 빠른 제어를 목표로 설계된다. |
| Safety | 0 | 처리 순서는 명확하지만 오류 발견이 다음 배치까지 늦을 수 있다. | + | 필터별 책임이 분리되어 오류 범위를 제한하기 쉽다. | ++ | 상태 감시와 보정이 가능해 안전 요구에 적합하다. |
| Security | 0 | 구조 자체는 단순하지만 중간 파일 보호가 필요하다. | 0 | 연결 통로마다 접근 제어가 필요해 별도 설계에 좌우된다. | - | 제어 명령이 공격받으면 물리적 피해로 이어질 수 있다. |
| Testability | ++ | 입력과 출력이 명확해 단계별 재현 테스트가 쉽다. | + | 각 필터를 독립적으로 단위 테스트하기 좋다. | - | 실제 환경과 시간 조건을 테스트로 재현하기 어렵다. |
| Usability | - | 사용자는 배치 완료까지 기다려야 해서 즉시성이 낮다. | 0 | 내부 처리 구조라 사용자 경험은 별도 UI 설계에 달려 있다. | 0 | 주로 내부 제어 구조라 사용자 편의성과 직접 관련은 작다. |

### Data Centered

| Quality Attribute | Shared Repository - Effects | Shared Repository - Justification | Blackboard - Effects | Blackboard - Justification |
|---|---|---|---|---|
| Availability | - | 중앙 저장소 장애가 전체 시스템 장애로 이어질 수 있다. | - | 블랙보드나 제어기가 멈추면 지식 소스가 함께 영향을 받는다. |
| Deployability | - | 저장소 스키마 변경이 여러 컴포넌트 배포와 연결된다. | + | 지식 소스는 공통 블랙보드 규칙만 맞추면 따로 배포할 수 있다. |
| Integrability | ++ | 여러 컴포넌트가 같은 데이터 모델로 쉽게 연동된다. | ++ | 서로 다른 지식 소스가 블랙보드를 통해 간접적으로 협력한다. |
| Energy Efficiency | + | 데이터를 한곳에 모아 중복 저장과 중복 처리를 줄일 수 있다. | - | 여러 지식 소스가 반복적으로 상태를 확인해 낭비가 생길 수 있다. |
| Modifiability | - | 공통 데이터 구조 변경이 많은 컴포넌트에 파급된다. | ++ | 새로운 지식 소스를 추가해 기능을 확장하기 쉽다. |
| Performance | - | 중앙 저장소에 요청이 몰리면 병목이 생긴다. | - | 블랙보드 접근과 조정 과정이 성능 부담을 만든다. |
| Safety | + | 단일 진실 원천으로 데이터 일관성과 추적성을 확보하기 쉽다. | 0 | 추론 과정은 추적 가능하지만 실행 순서가 복잡해질 수 있다. |
| Security | + | 중앙 저장소에서 접근 제어와 감사를 집중 관리할 수 있다. | - | 여러 지식 소스가 공유 지식에 접근해 권한 관리가 복잡하다. |
| Testability | - | 테스트 데이터와 공유 상태를 준비하고 격리하기 어렵다. | - | 지식 소스의 실행 순서와 조합 결과를 예측하기 어렵다. |
| Usability | 0 | 데이터 구조는 내부 설계라 사용자 편의성은 별도 화면에 좌우된다. | 0 | 문제 해결 품질에는 도움 되지만 사용성 자체를 보장하지 않는다. |

### Implicit Invocation

| Quality Attribute | Non-Buffered Event Based - Effects | Non-Buffered Event Based - Justification | Buffered Message Based - Effects | Buffered Message Based - Justification |
|---|---|---|---|---|
| Availability | - | 수신자가 없거나 장애 중이면 이벤트가 사라질 수 있다. | ++ | 메시지를 큐에 보관해 소비자 장애 후에도 처리할 수 있다. |
| Deployability | + | 발행자와 구독자를 독립적으로 배포하기 쉽다. | ++ | 생산자와 소비자가 큐를 기준으로 독립 배포된다. |
| Integrability | + | 이벤트 계약만 맞추면 새로운 구독자를 쉽게 연결할 수 있다. | ++ | 표준 메시지와 브로커로 이기종 시스템 연동이 쉽다. |
| Energy Efficiency | + | 저장 공간 없이 즉시 전달해 메시지 보관 비용이 적다. | 0 | 부하 평준화는 가능하지만 큐와 브로커 운영 비용이 든다. |
| Modifiability | ++ | 발행자를 바꾸지 않고 구독자를 추가하거나 제거할 수 있다. | ++ | 라우팅과 소비자를 바꿔도 생산자 영향이 작다. |
| Performance | + | 버퍼 없이 즉시 전달되어 지연 시간이 짧다. | + | 비동기로 부하를 흡수하지만 큐 대기 시간이 추가될 수 있다. |
| Safety | - | 이벤트 손실이나 순서 문제가 상태 오류로 이어질 수 있다. | + | 내구성 있는 메시지와 재시도로 손실 위험을 줄인다. |
| Security | - | 이벤트 흐름이 분산되어 인증과 감사 범위가 넓어진다. | + | 브로커에서 인증, 권한, 감사를 집중 관리할 수 있다. |
| Testability | - | 이벤트 타이밍과 순서 때문에 테스트 재현성이 낮다. | + | 메시지를 저장하고 재처리할 수 있어 시나리오 검증이 쉽다. |
| Usability | 0 | 빠른 반응은 가능하지만 사용자 경험은 이벤트 처리 설계에 달려 있다. | + | 오래 걸리는 작업을 백그라운드로 처리해 화면 응답성을 높인다. |

### Interaction-Oriented

| Quality Attribute | MVC - Effects | MVC - Justification | PAC - Effects | PAC - Justification |
|---|---|---|---|---|
| Availability | 0 | 화면 구조 분리는 도움 되지만 장애 복구를 직접 보장하지는 않는다. | + | 에이전트 단위로 분리되어 일부 장애를 격리하기 쉽다. |
| Deployability | + | Model, View, Controller를 나누어 UI와 로직 변경을 따로 배포하기 쉽다. | + | 독립 에이전트 단위로 기능을 추가하거나 배포하기 좋다. |
| Integrability | + | Model과 Controller의 인터페이스를 통해 다른 화면이나 기능과 연결하기 쉽다. | + | 에이전트 간 제어 인터페이스로 독립 기능을 조합할 수 있다. |
| Energy Efficiency | 0 | 화면 갱신 구조는 명확하지만 Observer나 동기화 비용이 생길 수 있다. | - | 여러 에이전트와 제어 계층이 통신해 추가 비용이 발생한다. |
| Modifiability | ++ | UI, 입력 처리, 데이터 로직을 분리해 변경 영향이 작다. | ++ | 에이전트별 책임이 분리되어 기능 추가와 수정이 쉽다. |
| Performance | 0 | 역할 분리의 간접 호출은 있지만 일반적으로 큰 병목은 아니다. | - | 에이전트 간 조정과 메시지 전달이 응답 시간을 늘릴 수 있다. |
| Safety | 0 | 입력 검증 위치는 명확하지만 안전 기능 자체를 제공하지는 않는다. | + | 에이전트 경계로 오류 전파를 줄이고 제어 책임을 분리할 수 있다. |
| Security | + | Controller에서 입력 검증과 접근 제어를 집중 적용하기 쉽다. | 0 | 에이전트별 보안 적용은 가능하지만 통신 경로 관리가 필요하다. |
| Testability | + | Model과 Controller를 View와 분리해 단위 테스트하기 쉽다. | 0 | 개별 에이전트는 테스트 가능하지만 상호작용 테스트는 복잡하다. |
| Usability | ++ | 여러 View와 빠른 UI 변경을 지원해 사용자 경험 개선에 유리하다. | + | 복잡한 인터랙션을 에이전트별로 나누어 화면 구성이 유연하다. |

### Hierarchical - Main-subroutine / Master-Slave

| Quality Attribute | Main-subroutine - Effects | Main-subroutine - Justification | Master-Slave - Effects | Master-Slave - Justification |
|---|---|---|---|---|
| Availability | - | 상위 호출 흐름이 멈추면 하위 기능도 함께 멈추기 쉽다. | 0 | Slave 장애는 대체 가능하지만 Master가 단일 장애점이 될 수 있다. |
| Deployability | - | 기능이 하나의 호출 구조에 묶여 부분 배포가 어렵다. | + | Slave를 추가하거나 교체해 처리 능력을 단계적으로 배포할 수 있다. |
| Integrability | - | 상위와 하위 모듈이 호출 관계로 강하게 연결된다. | 0 | Master-Slave 프로토콜이 맞아야 하므로 연동 자유도는 제한된다. |
| Energy Efficiency | + | 직접 함수 호출 중심이라 구조적 실행 오버헤드가 작다. | 0 | 병렬 처리로 효율을 높일 수 있지만 여러 Slave 운영 비용이 든다. |
| Modifiability | - | 호출 계층 변경이 상위와 하위 모듈에 쉽게 전파된다. | 0 | Slave 교체는 쉽지만 Master의 분배 로직 변경 영향이 크다. |
| Performance | + | 단순한 제어 흐름과 직접 호출로 실행 속도가 빠르다. | ++ | 작업을 여러 Slave에 나누어 병렬 처리 성능을 높일 수 있다. |
| Safety | 0 | 실행 흐름은 예측 가능하지만 오류 격리 장치는 약하다. | + | 여러 Slave 결과를 비교하거나 대체해 오류를 줄일 수 있다. |
| Security | 0 | 구조가 단순해 분석은 쉽지만 별도 보안 메커니즘은 없다. | - | Master가 공격받으면 전체 Slave 제어가 영향을 받을 수 있다. |
| Testability | + | 각 서브루틴의 입력과 출력이 명확해 단위 테스트가 쉽다. | 0 | Slave 단위 테스트는 쉽지만 분산 실행 테스트는 추가 노력이 든다. |
| Usability | 0 | 내부 구현 구조라 사용자 경험에는 직접 영향이 작다. | 0 | 처리 방식은 내부 구조라 사용자 편의성은 별도 UI에 달려 있다. |

### Hierarchical - Layered / Virtual Machine / Microkernel

| Quality Attribute | Layered - Effects | Layered - Justification | Virtual Machine - Effects | Virtual Machine - Justification | Microkernel - Effects | Microkernel - Justification |
|---|---|---|---|---|---|---|
| Availability | 0 | 하위 계층 장애는 전파되지만 계층별 격리로 영향 범위를 줄일 수 있다. | + | 실행 환경 격리와 복구 기능으로 장애 영향을 줄이기 쉽다. | + | 핵심 기능과 서버를 분리해 일부 서비스 장애를 격리할 수 있다. |
| Deployability | + | 계층 인터페이스가 안정적이면 계층별 배포가 가능하다. | ++ | 동일한 VM 위에서 다양한 환경에 쉽게 배포할 수 있다. | + | 개별 서버 기능을 독립적으로 추가하거나 갱신하기 쉽다. |
| Integrability | + | 계층별 표준 인터페이스로 상위 기능을 쉽게 연결할 수 있다. | + | VM API를 기준으로 여러 프로그램을 같은 방식으로 실행할 수 있다. | ++ | 메시지 기반 인터페이스로 다양한 서비스와 드라이버를 연결하기 좋다. |
| Energy Efficiency | - | 여러 계층을 거치며 호출과 데이터 변환 비용이 생긴다. | - | 가상화나 해석 실행으로 네이티브 실행보다 비용이 커질 수 있다. | - | 서비스 간 IPC와 컨텍스트 전환 비용이 반복해서 발생한다. |
| Modifiability | ++ | 특정 계층을 교체해도 인터페이스가 유지되면 영향이 작다. | + | 플랫폼 차이를 VM이 숨겨 하위 환경 변경 영향을 줄인다. | ++ | 커널을 작게 유지하고 서버를 추가해 기능 확장이 쉽다. |
| Performance | - | 계층을 순서대로 통과해 직접 호출보다 지연이 생긴다. | - | 가상화 계층 때문에 실행 오버헤드가 발생할 수 있다. | - | 메시지 전달과 컨텍스트 전환이 성능 병목이 될 수 있다. |
| Safety | + | 계층별 책임과 접근 경로가 명확해 오류 전파를 줄일 수 있다. | ++ | 샌드박스와 격리로 위험한 실행을 제한하기 좋다. | + | 작은 커널과 서비스 격리로 오류 범위를 줄일 수 있다. |
| Security | + | 보안 정책을 특정 계층에 집중 적용하기 쉽다. | ++ | VM 샌드박스가 실행 권한과 자원 접근을 제한할 수 있다. | ++ | 작은 신뢰 기반과 서비스 격리로 공격 표면을 줄일 수 있다. |
| Testability | + | 계층별 Mock을 사용해 단위와 통합 테스트를 나누기 쉽다. | + | 동일한 VM 환경에서 테스트를 재현하기 쉽다. | + | 작은 커널과 개별 서버를 분리해 테스트하기 좋다. |
| Usability | 0 | 내부 구조라 사용자 편의성은 상위 기능 설계에 좌우된다. | + | 사용자에게 일관된 실행 환경을 제공해 사용 혼란을 줄인다. | 0 | 내부 시스템 구조라 최종 사용성에는 직접 영향이 작다. |
